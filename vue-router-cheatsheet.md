1. Add a link - '<router-link to="">'

2. To change location in script
   this.$router.push({ name: 'sources', params: })

3. Router looks like this 
     routes: [
    {
      path: '/',
      name: 'home',
      component: Home,
    },
